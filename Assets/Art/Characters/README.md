# Character sprites (placeholder)

Strip PNGs `player_sheet.png` / `enemy_sheet.png` are generated by `CharacterAnimationPipeline.GenerateAll` (Editor / batchmode). Replace with real idle/walk/attack frames (same slice layout: 8×32px frames in one row) and re-run the pipeline or update Animation clips in the Editor.
